# Decisions log

Every choice that changes the data or results, with a date and reason. Examiners ask "why?"; this file answers.
Newest first. Add a row whenever you change `configs/config.yaml` or a rule in the code.

| Date | Decision | Reason | Changed by |
| --- | --- | --- | --- |
| 2026-09-23 | Loughran-McDonald dictionary version: **Master Dictionary 1993-2025** (86,553 words; the edition sraf.nd.edu linked on 2026-09-23, noted there as updated March 2026), saved as `data/manual/loughran_mcdonald.csv`, SHA-256 `e2d13286...f4859d` | Records the version, as the row below requires; tone numbers from another edition are not comparable | Gaurav (approved download) + Claude |
| 2026-09-23 | The download agent (1.4) keeps a file only when it is complete: the byte count matches `Content-Length` when sent, and a PDF ends with `%%EOF`; otherwise it downloads again, up to three times | One annual report (BSE542323, FY2020) arrived as 98 KB of a 3.9 MB file with HTTP 200 and passed the old `%PDF` header check. All 209 pilot PDFs were re-checked; that was the only one | Claude (pilot) |
| 2026-09-23 | An exchange XBRL filing is **dropped for that company-year** when its total assets differ from the annual report's by a power of ten (x100, x1,000, x1 lakh, x1 crore) and the report states its own unit; above Rs 20 lakh crore it is always treated as a unit error | Companies file XBRL in the wrong unit: Rs 7.5 crore of total assets was filed as Rs 7.5 lakh crore. XBRL-first would otherwise have put the error straight into the ratios | Claude (Phase 3, real reports) |
| 2026-09-23 | Statement reading rules added after the first real reports: rupee statements are recognised from an `Amount (Rs.)` column header or from large whole numbers; the P&L and cash-flow statement are the ones after the balance sheet; a figure found in the wrong statement is dropped (cash from the cash-flow statement excepted); expenses printed in brackets are stored positive; PBT is the figure after exceptional items | Each rule fixes a failure seen on real reports: small companies print in rupees with no caption (values 10^7 too large); the directors' report reprints a P&L summary; cash-flow movements were read as balance-sheet items; bracketed finance costs came out negative; pre-exceptional PBT was taken. PDF-vs-XBRL agreement within 1% rose from 64% to 85% | Claude (Phase 3, real reports) |
| 2026-09-23 | Real data is collected by a small download agent (`bpp_fetch.py`) running on a team laptop, fed job files written from the analysis session | The exchanges and IBBI block cloud addresses; the laptop has an Indian address. The agent is standard-library Python, only contacts IBBI/BSE/NSE, only writes inside its own folder and never runs what it downloads. Every request is logged with its checksum | Gaurav + Claude |
| 2026-09-23 | IBBI data comes from IBBI's own export of CIRP announcements (one request), not the scraped listing pages | The export carries each debtor's CIN, which the listing pages do not | Claude (Phase 1) |
| 2026-09-23 | **Listed firms are identified by CIN** (`L` = listed at some point), matched to BSE/NSE by name with the same scorer and thresholds as before, and each match is confirmed by the CIN printed in the company's own annual report. People review only mismatches and firms without a CIN | Replaces fuzzy matching of all 8,579 debtors plus a manual review of every 80-97 score. Of 603 L-CIN debtors, 433 match at >= 98; the report CIN settles the rest exactly | **Gaurav (approved)** |
| 2026-09-23 | **Industry = BSE's classification** as a hierarchical code `IN`+sector(2)+industry(2)+group(2)+sub-group(3). Peers share the sub-group, else the group (`industry_fallback_prefix_len` 8 for this code) | Available for every active listed company from the exchange, and identical for both classes. CIN NIC codes are set at incorporation and exist only for the insolvent side | **Gaurav (approved)** |
| 2026-09-23 | **Financial figures: exchange XBRL first** (`financials.xbrl_priority: first`). The audited standalone annual results filed in XBRL (BSE from FY2019, NSE from FY2018) are used where they exist; the annual-report PDF supplies older years and gaps, and is compared with XBRL wherever both exist | XBRL figures are tagged line by line in one taxonomy - the closest public substitute for CMIE Prowess - and are filed in May, before the annual report, so they are the first publication. The PDF-vs-XBRL comparison measures the PDF reader's accuracy for free | **Gaurav (approved)** |
| 2026-09-23 | Total assets for peer matching come from the XBRL annual filing of the reference FY, or the FY before when the reference year was never filed | A firm about to enter insolvency often stops filing; the year before is the last honest size | Claude (Phase 1) |
| 2026-09-23 | Report publication date = BSE's filing time where recorded; otherwise FY end + 183 days, flagged `assumed` (the existing rule) | BSE records a filing time for almost no report before FY2023 | Claude (Phase 2) |
| 2026-09-23 | Pilot sample: 40 insolvent firms admitted 2019-2025 (XBRL coverage for sizing), stratified by admission year, at most two per sub-group, seed 42; peers chosen from each sub-group's members traded on the collection day | Tests every step end to end before scaling, as the Phase 3 brief asks. The traded-members list (max 30 per sub-group) is only for the pilot; the full run uses the full classification of every active company | Claude (pilot) |
| 2026-09-23 | Pilot excludes financial-sector firms, government companies, and BSE group A firms. **Provisional - to be confirmed by the team before the full run** | Financial firms have no current/non-current balance sheet, so most ratios do not exist. Admissions against PSUs and group-A companies were stayed within days in every case seen (Container Corporation 2019, Titagarh Wagons 2022, Zee Entertainment 2023): a healthy firm labelled distressed | Claude (pilot) |
| 2026-09-23 | Page text in cloud sessions is produced with pypdfium2 (`extract/pdf_text_pdfium.py`) into the same `pages/<doc_id>.json`; Phase 2 code unchanged | PyMuPDF cannot be installed in the cloud sessions. The `backend` field records which extractor made each file | Claude (Phase 2 support) |
| 2026-09-23 | Phase 4 stream B built; stream A (FinBERT) left for Colab | Tone, hedging, readability, auditor flags and drift need no GPU and can be tested on a laptop. Splitting them out means the testable part is not blocked on the untestable part | Claude (Phase 4) |
| 2026-09-23 | The Loughran-McDonald dictionary is **not** vendored; it is read from `data/manual/loughran_mcdonald.csv` and its version recorded | It is republished yearly and tone ratios are not comparable across versions. A silent copy in the repo would make a reported tone number irreproducible | Claude (Phase 4) |
| 2026-09-23 | Tone ratios use the raw word count as the denominator, not the stopword-stripped one | Loughran and McDonald's own methodology. A ratio on a filtered denominator is not comparable with any published figure, including the 2.21% vs 1.30% in Gupta & Banerjee (2023) that this project is measured against | Claude (Phase 4) |
| 2026-09-23 | CARO flags are negation-aware, judged clause by clause | Every clean annexure says "the Company has **not** defaulted", "**Neither** the Company **nor** its promoters ... wilful defaulter" and "there are **no** undisputed statutory dues outstanding". A flag matching the phrase alone is 1 for every company in the sample: it looks like a working feature and carries no information | Claude (Phase 4) |
| 2026-09-23 | "going concern", "lenders have" and "overdue" removed from the distress phrase seed list | SA 570 puts "management's use of the going concern basis of accounting" in every unmodified auditor's report, so the feature measured report length rather than distress | Claude (Phase 4) |
| 2026-09-23 | Words are tokenised on runs of letters, so hyphenated compounds split, and a hyphen at a line break is rejoined | "non-performing" as one token can never match the dictionary, and it is exactly the vocabulary being looked for. PDF line-break hyphens otherwise inflate every word count | Claude (Phase 4) |
| 2026-09-23 | Sentence splitting treats line breaks, headings and bullets as boundaries | A PDF-extracted MD&A is largely headings and bullets. A splitter needing a full stop returns one sentence for the whole section, so average sentence length equals the word count and Fog reads ~38 for every such document | Claude (Phase 4) |
| 2026-09-23 | Perplexity uses interpolated Kneser-Ney with true continuation counts at every order below the highest | Without them it is absolute discounting with a Kneser-Ney unigram -- a different model under the same name, and the "crore, frequent but always in the same phrase" problem it was chosen to solve stays unfixed | Claude (Phase 4) |
| 2026-09-23 | Perplexity is left empty unless a training set is named with `--lm-train-doc-id`; documents used to fit the model are marked | Fitting the reference model on every report leaks the test fold into training, exactly as a globally fitted scaler would. A document scored by a model fitted on it is flattered | Claude (Phase 4) |
| 2026-09-23 | Drift is keyed by `doc_id`, comparing a firm with **itself** a year earlier | Two documents can share a firm-year (a revised filing, a re-extract), and a `(firm, fy)` key silently hands one document the other's drift. A first year has empty drift, not zero: zero would read as "wrote exactly the same thing again" | Claude (Phase 4) |
| 2026-09-23 | The stream-B model input is the 25 values in `CORE_LANGUAGE_FEATURES`; the table carries more | Matches Table 7 and the `25 -> 64 -> 64` layer. The extra columns exist for the Phase 7 MD&A/auditor ablations, but with ~100 insolvent firms a 54-wide input into a 64-unit layer is the overfitting the plan warns about | Claude (Phase 4) |
| 2026-09-23 | Phase 3 figures come from the annual-report PDFs, not CMIE Prowess. MCA21 is not used | Prowess access was never confirmed with the library; the reports are already collected in Phase 2 | Claude (Phase 3) |
| 2026-09-23 | Gap-filling order: (1) the next year's prior-year comparative, (2) `manual/xbrl_financials.csv` from NSE/BSE XBRL annual results, (3) mark missing and apply the pair-exclusion rule | Uses what we already have before asking anyone to type figures in; the count of unrecoverable company-years is reported by class every run | Claude (Phase 3) |
| 2026-09-23 | Figures stored **as first published**; the next year's comparative may cross-check or fill a gap, never overwrite. A difference beyond 0.5% sets `restated` | The model must see what the market could read at the time. A restated figure is hindsight, which is leakage | Claude (Phase 3) |
| 2026-09-23 | Gap-fill step 2 is a hand-filled CSV, not an XBRL scraper | The exchange endpoints block cloud addresses and the BSE one is still unverified; an untested scraper would give false confidence. Same pattern as `manual_reports.csv` | Claude (Phase 3) |
| 2026-09-23 | Statements are read in **₹ crore**, with the figure as printed and its declared unit kept alongside | Matches `firm_financials.csv` and the rest of the project; keeping the printed figure makes a scale error detectable afterwards | Claude (Phase 3) |
| 2026-09-23 | Standalone statements preferred over consolidated; consolidated used only when no standalone set exists, and flagged | Consolidated statements include subsidiaries, so they are the wrong denominator for every ratio. Same rule as the auditor's report and CARO (16 Sep) | Claude (Phase 3) |
| 2026-09-23 | ROA = net profit / total assets, not EBIT / total assets | Keeps the ratio comparable with Gupta (2022), the Indian IBC benchmark this project measures itself against | Claude (Phase 3) |
| 2026-09-23 | A ratio with a negative or zero denominator returns empty with a reason; the condition is kept as its own feature (`negative_equity`, `negative_ebitda`, `zero_finance_costs`, …) | Distressed Indian firms routinely have negative equity and EBITDA. Debt/equity of −5.3 on wiped-out equity would sort as healthier than +8.0 on thin equity, teaching the model the opposite of the truth | Claude (Phase 3) |
| 2026-09-23 | Altman: report both `altman_z_em` = 6.56·X1 + 3.26·X2 + 6.72·X3 + 1.05·X4 + **3.25** (the rating-equivalent EMS scale) and `altman_z_dprime` = the same without the constant. The safe/grey/distress zones of 2.6 / 1.1 are applied to **Z'' (no constant)**. Altman X4 keeps a negative value | Altman, Hartzell & Peck (1995). The +3.25 exists only so that EMS = 0 is a D rating; it carries no information. Popular sources print the 2.6/1.1 cutoffs next to the +3.25 formula, which shifts every firm up by 3.25 — a firm with negative working capital and near-zero EBIT then scores "safe". Z'' is linear, so negative equity should push the score down rather than void it. (Some secondary sources also print 6.5 for X1; 6.56 is the published coefficient) | Claude (Phase 3) |
| 2026-09-23 | An explicitly empty money column (dash, `Nil`) is tokenised as a column rather than skipped | Statement lines are positional. With the blank invisible, `Borrowings 14 500.00 -` reads the note reference 14 as this year's figure and files this year's 500 against last year — silently, at full confidence | Claude (Phase 3) |
| 2026-09-23 | A scale is only believed from a caption ("(Rs. in lakhs)"), the earliest such mention wins, short forms ("Cr") need a qualifier, and two disagreeing scales lower the confidence | "Turnover crossed Rs. 500 crore" in prose, and the "Cr" of a Dr/Cr column header, both used to override the real caption and make every figure 100x too large | Claude (Phase 3) |
| 2026-09-23 | A statement heading is only accepted when its tail is short and the page carries at least `min_money_lines` figures | The auditor's report quotes the balance sheet's title ("…which comprise the Balance Sheet as at March 31, 2019, and…") on an earlier page, and was winning the location | Claude (Phase 3) |
| 2026-09-23 | `Total liabilities` is its own field, not `Total equity and liabilities`; a reported figure is preferred for Altman X4 | In Schedule III they are different quantities. Conflating them made the balance-sheet check fail on correct reports and cost the whole company-year its confidence | Claude (Phase 3) |
| 2026-09-23 | Borrowings summed from one side only are flagged `borrowings_partial` | Short-term borrowings only map when the sub-heading was found, so a half-mapped balance sheet is routine and would silently understate leverage | Claude (Phase 3) |
| 2026-09-23 | EBIT = PBT + finance costs; EBITDA = EBIT + depreciation; capital employed = reported figure, else total assets − current liabilities | Built from what Schedule III actually prints on the face of the statements | Claude (Phase 3) |
| 2026-09-23 | `Borrowings` is resolved by its Schedule III sub-heading (non-current vs current), not by the label | Schedule III prints the identical label twice; only the context above it distinguishes long- from short-term debt | Claude (Phase 3) |
| 2026-09-23 | Line-item matching uses stdlib `difflib`, not `rapidfuzz` | Labels are short and patterns do most of the work; avoids making Phase 3 depend on a library it does not need | Claude (Phase 3) |
| 2026-09-23 | pdfplumber and camelot are optional (`pip install -e ".[tables]"`); the text walk always runs | A scanned statement has no table structure, so the text path must work alone. Tables only improve accuracy on ruled statements | Claude (Phase 3) |
| 2026-09-23 | Ratios are stored raw; winsorising at the 1st/99th percentile and scaling are fitted inside the CV folds | Bounds learnt over the whole table leak the test fold's distribution into training, exactly as a globally fitted scaler would | Claude (Phase 3) |
| 2026-09-23 | A company-year with no usable figures keeps its row, flagged; its pair partner's same year is excluded from modelling | Missing and late filing is itself a distress signal (project plan, issue 7). Aligning pairs stops the model separating the classes on calendar year | Claude (Phase 3) |
| 2026-09-23 | `synthetic.py` extended with `financial_statements=False`, off by default | Phase 3 tests need real Schedule III statements; defaulting it off leaves every existing Phase 1–2 test and `bpp demo` untouched. Agreed with the team before changing a Phase 1–2 file | Team + Claude |
| 2026-09-16 | Admission date proxy = earliest IBBI CIRP public-announcement date; replace with verified insolvency commencement date where checked | IRP must publish Form A within ~3 days of appointment; IBBI list is complete and public | Claude (setup) |
| 2026-09-16 | Name matching: auto-accept only at fuzzy score ≥ 98; 80–97 goes to human review | In testing, "Tarang Infra" vs "Tarangi Infra" scored 96; a wrong match puts a healthy firm in the distressed class | Claude (setup) |
| 2026-09-16 | Any firm appearing in the CIRP match list (auto or needs-review, unless reviewed N) is excluded from healthy peers | Conservative: a possibly-distressed firm must never be labelled healthy | Claude (setup) |
| 2026-09-16 | Peers: same industry code (fallback first 2 digits), total assets ±30% in the last FY before admission, closest by log assets, each peer used once, scarcest firms matched first | Standard matched-sample design (industry + size), as in the Business Perspectives (2023) IBC study | Claude (setup) |
| 2026-09-16 | Collect 4 FYs per firm, keep 3 (t-1..t-3) | The most recent report is often excluded by the leakage rule | Claude (setup) |
| 2026-09-16 | Exclude reports published after the petition date, or within 180 days before admission when the petition date is unknown | Such reports can openly discuss the insolvency case (label leakage) | Claude (setup) |
| 2026-09-16 | Keep pairs aligned: drop the peer's FY when the distressed firm's FY is excluded or missing | Prevents the model from learning calendar year as a shortcut | Claude (setup) |
| 2026-09-16 | Unknown publication date = FY end + 183 days, flagged `assumed` | AGM must be held within 6 months of FY end (Companies Act, 2013) | Claude (setup) |
| 2026-09-16 | Standalone auditor's report preferred over consolidated | CARO applies to standalone statements; consolidated reports repeat subsidiaries' matters | Claude (setup) |
| 2026-09-16 | Section extraction is rule-based (regex headings + run logic), accuracy hand-checked on 30 reports | Explainable, fast, no training data needed; accuracy is reportable | Claude (setup) |
| 2026-09-16 | Laptops (VS Code) for Phases 0–3; Colab GPU for Phases 4 and 6; GitHub for code; Google Drive for data | Exchange sites block cloud IPs; no local NVIDIA GPU | Team + Claude |
